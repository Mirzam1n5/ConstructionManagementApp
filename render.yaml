services:
  - type: web
    name: isker-app
    runtime: node
    buildCommand: npm install && npm run build:web
    staticPublishPath: dist
    envVars:
      - key: NODE_VERSION
        value: "20"
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
